import * as transition from './transitions';

describe('transition', () => {
  it('contains available states', () => {
    expect(transition.transitions).toBeDefined();
  });

  it('contains transitions', () => {
    expect(transition.transitions).toBeDefined();
  });
});